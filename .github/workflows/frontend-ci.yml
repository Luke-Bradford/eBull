name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci.yml"
  # No ``branches:`` filter on pull_request — see #2783.
  pull_request:
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: frontend/.nvmrc
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Dark-mode class gate (#708)
        run: pnpm dark:check

      - name: Hand-rolled status pill gate (#2148)
        run: pnpm pills:check

      - name: Chart-integrity gate (#2185)
        run: pnpm charts:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Run frontend tests (#247)
        # Full ``pnpm test`` includes the SetupPage integration tests
        # — heavier but representative of the user-visible path. The
        # local pre-push gate uses ``test:unit``; CI runs the wider
        # ``test`` so an integration-only regression cannot ship
        # behind a green pre-push.
        run: pnpm test
