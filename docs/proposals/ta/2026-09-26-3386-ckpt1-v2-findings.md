# #3386 spec v2 — Codex ckpt-1 scoped round (verbatim)

Checked all 52. Omitted findings adequately fixed, bound to a slice, or declared residuals. **S** = [v2 spec](2026-09-26-3386-hunt-feature-store.md).

1. **v1 #2 — incomplete; S74–80.** Quarantine counts do not identify masking. Moving a false flag between dates, or changing overlap between range/return failures, preserves counts while changing loaded prices and exclusions.

2. **v1 #3 — incomplete; S79–80,185–189.** Interior bar-date changes remain omitted from the residual. They can preserve counts/ranges while changing session alignment; the claim to detect “series shape” is too broad.

3. **v1 #7 — incomplete; S87–91,171.** Identity checking still precedes publication and computation. Changes during readback escape that check; a later harness rejection still leaves a published cache. No rejection-triggered invalidation is specified.

4. **v1 #24 — incorrect guarantee despite binding; S90–91.** Rename without file/directory durability steps does not guarantee “no entry or a whole one” after a system crash. Distinguish process interruption from durable publication.

5. **v1 #27 — incomplete; S71–73,99–102.** The discovery check remains caller-only. Store write/read operations have no independent cutoff check tying `through`, manifest sessions and supplied parts to discovery.

6. **v1 #28 — incomplete; S108–112.** The revised enforcement claim still omits the gated-script exceptions identified in v1. Adding a reader-module entry does not remove those exceptions.

7. **v1 #51 — unsupported estimate despite binding; S64–65,94–95.** Releasing live parts does not establish the asserted ~2× peak: byte buffers, decoded arrays, reconstructed `array` columns and series indices can overlap.

8. **NEW — quarantine identity is not trial-bound; S71–78,104–106.** It enters the store key, but no change binds it before registration. Changed quarantine metadata can therefore produce different inputs under the same registered trial identity.

9. **NEW — quarantine drift on hits; S83–88,97.** Quarantine identity is rechecked only during builds. No hit-path or post-computation recheck is specified; the existing universe-identity check does not cover this newly separate identity.

10. **NEW — quarantine scope; S74–76.** No `through`/loaded-coverage restriction is specified for quarantine counts. Changes affecting only later or unloaded bars can invalidate discovery caches or reject builds without changing their inputs.

11. **NEW — identity handoff; S40–42,71–78.** `compute_trial` currently receives neither the registered universe identity nor model ID. Their handoff into `hunt_panel` is unspecified; independently recomputing identity after registration can bind the cache to a different snapshot.

12. **NEW — failed discovery guard; S99–102.** Behavior for `split == "discovery"` with the wrong end is unspecified. Falling through to live loading would still read protected dates; this case needs explicit refusal before loading.

13. **NEW — dividend representation; S123–129,168.** Live `PanelSeries.dividends` is a mapping, while the view assumes parallel arrays. Specify conversion and ownership once per loaded series; rebuilding arrays per formation defeats the existing lazy-view complexity contract.

14. **NEW — live-path outcome schema; S99–105.** Store identifiers are promised in `statistics["load"]`, but validation/holdout never use a store. Their absent/null field contract is unspecified.

15. **NEW — canonical JSON contract; S60–62,71–73.** “Every value a string or integer” conflicts with nested `load_counts`, `termination_classes` and `universe_identity.form()`. Specify permitted containers and scalar leaves.
